{
  "name": "redmine-playwright-tests",
  "version": "1.0.0",
  "description": "Playwright automation tests for redmine.org with POM architecture and Allure reporting",
  "type": "module",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "report": "npx allure generate allure-results --clean -o allure-report && npx allure open allure-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.9.3",
    "allure-commandline": "^2.27.0",
    "allure-playwright": "^3.0.0"
  }
}